[project]
name = "bytestream"
version = "0.1.0"
description = "A small framed TCP client-server protocol."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
